"""
Урок 1.2: Типы данных — Домашнее задание.

Твоя задача — создать переменную нужного типа или выполнить операцию.
Строчки с def и return уже написаны за тебя, их не трогай.
Когда всё готово — запусти тесты: pytest test_lesson_1_2.py -v
"""


def task_1() -> int:
    """
    Создай переменную age — целое число 25.
    """
    age = 0         # ← положи целое число 25
    return age


def task_2() -> float:
    """
    Создай переменную price — дробное число 99.90.
    """
    price = 0.0     # ← положи дробное число 99.90
    return price


def task_3() -> str:
    """
    Создай переменную name — строку "Анна".
    """
    name = ""       # ← положи строку "Анна"
    return name


def task_4() -> bool:
    """
    Создай переменную is_admin — True.
    """
    is_admin = False    # ← положи True
    return is_admin


def task_5() -> int:
    """
    Дана строка text = "42". Преобразуй её в целое число.
    """
    text = "42"
    number = 0      # ← преобразуй text в целое число
    return number


def task_6() -> int:
    """
    Сложи числа 10 и 5.
    """
    result = 0      # ← сложи 10 и 5
    return result


def task_7() -> str:
    """
    Склей строку "Привет, " со строкой "Анна".
    """
    result = ""     # ← склей две строки
    return result


def task_8() -> int:
    """
    Найди остаток от деления 10 на 3.
    """
    result = 0      # ← остаток от деления
    return result


def task_9() -> str:
    """
    Повтори строку "ха" три раза.
    """
    result = ""     # ← повтори строку
    return result
