# Итог: когда что использовать

---

## Шпаргалка

```python
# list — упорядоченный, изменяемый
fruits = ["яблоко", "банан"]
fruits.append("киви")
first = fruits[0]

# tuple — упорядоченный, неизменяемый
point = (10, 20)
x = point[0]

# dict — ключ → значение
user = {"name": "Анна", "age": 25}
user["city"] = "Москва"
name = user["name"]

# set — только уникальные, без порядка
tags = {"python", "fastapi"}
tags.add("docker")
has_python = "python" in tags
```

---

## Правило большого пальца

| Ситуация | Бери |
|----------|------|
| Набор однотипных штук (фрукты, имена, числа) | `list` |
| Набор, который не должен меняться (координаты, настройки) | `tuple` |
| Данные с именами полей (пользователь, товар, запрос API) | `dict` |
| Уникальные значения, проверка «есть или нет» (теги, роли) | `set` |

---

## Домашнее задание

👉 Открой [`data_collections.py`](./data_collections.py) — 6 заданий. В каждом описано **что** сделать, а **как** — из теории выше.

👉 Запусти тесты:

```bash
pytest test_lesson_1_3.py -v
```

---

## Не парься

Четыре коллекции за раз — это много. Нормально, если пока не запомнил всё наизусть. В следующих уроках мы будем постоянно их использовать — въедутся сами. Главное — теперь знаешь, что они есть и куда подглядеть.

---

Когда готово: [Урок 1.4: Циклы](../lesson%201.4/lesson_1.4_0.md)
